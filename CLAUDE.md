# Zuma Deluxe — VDAC2 / FT812 порт (640×480)

Параллельный проект Zuma. Соседнее окно ведёт VDC TS-Conf версию в `c:/z80/zuma/` (нативный 360×288).

## Cross-project chat
Общий append-only канал общения с другим окном Zuma:
**`C:/Users/Администратор/Desktop/Zuma Deluxe VDAC2/Чат.txt`** (тут же, в корне проекта)

- При **старте сессии** прочитать целиком.
- При **критической находке** (root-cause бага, архитектурное решение, фикс) — append entry по формату из шапки файла. Маркер `VDAC2 → VDC` или `VDAC2 ↔ VDC`.
- Ephemeral state (текущая задача) — НЕ туда.
- Не удалять; устаревшее помечать `[OBSOLETE: причина]`.
